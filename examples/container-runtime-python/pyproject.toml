[project]
name = "osva-container-runtime-python-example"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "osva",
]

[tool.setuptools]
py-modules = ["main"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
