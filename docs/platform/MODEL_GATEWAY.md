# Model Gateway

ModelProfiles provide provider-neutral model configuration.

Runs bind to ModelProfileVersions.

Provider adapters normalize requests, responses, usage, errors, and latency.

Stage 1 starts with one provider.

Stage 2 adds multiple providers.

OSS 1.0 adds richer routing and policy hooks.
