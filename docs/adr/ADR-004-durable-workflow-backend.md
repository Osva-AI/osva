# ADR-004: Durable Workflow Backend as an Adapter

**Status:** Accepted

OSVA owns Workflow definitions.

A durable workflow engine may execute them through `DurableWorkflowBackend` without becoming the public Workflow format.
